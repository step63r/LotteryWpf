<UserControl x:Class="LotteryWpf.Content.Views.TopPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1024"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox Text="{Binding CurrentUserName}" Width="200" Margin="0,0,0,10" />
            <Button Content="開始" Command="{Binding StartCommand}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" />
        </StackPanel>
    </Grid>
</UserControl>
